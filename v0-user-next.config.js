/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/Care-Plan-Homepage-Demo", // GitHubリポジトリ名に変更してください
}

module.exports = nextConfig

